/**
 * 
 * Récupérer le input et dire qu'il est vide | input.value = "";
 * 
 * créer un tableau pour stocker les taches
 *          ( notion de persistance doit permettre de sauvegarder les éléments passés)
 * 
 * const todos = [];
 * 
 * function addTodo(event) {
 *  if(event.key === "Enter") {
 *      const todo = todoAdd.value;
 *      todos.push(todo);
 * 
 *      insertTodo(); // Doit servir à insérer les li dans le DOM
 * 
 *      todoAdd.value = "";
 *  }
 * }
 * 
 * 
 * function insertTodo () {
 * 
 *  const LiElement = document.createElement("li");
 *  LiElement.classList.add("todo");
 * 
 * 
 * 
 * }
 * 
 * 
 * function updateStatus // Pour vérifier s'il y a un element dans le tableau
 */



